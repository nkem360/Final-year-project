# Copy this file to .env and fill in the values

# Backend API URL — leave blank to use the Vite dev proxy (recommended for local dev)
# Set this only if your backend runs on a different domain (e.g. production)
VITE_API_URL=
